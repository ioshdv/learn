DB_NAME=taskflow
DB_USER=user
DB_PASSWORD=change-me

JWT_SECRET=change-me-super-long
JWT_REFRESH_SECRET=change-me-super-long-refresh

# DB sync control in prod: true|alter|force (empty/false disables)
DB_SYNC=false

# URL publica desde donde se consume la API en prod
PUBLIC_BASE_URL=http://localhost
